actual = np.expm1(valid[target_col].values)
pred = np.expm1(valid["naive_pred"].values)

mask = (
    np.isfinite(actual) &
    np.isfinite(pred) &
    (actual > 1e-6)
)

print(
    f"Horizon {h}: "
    f"Total={len(actual)}, "
    f"Valid for MAPE={mask.sum()}, "
    f"Actual zero={(actual == 0).sum()}, "
    f"Actual finite={np.isfinite(actual).sum()}, "
    f"Prediction finite={np.isfinite(pred).sum()}"
)
