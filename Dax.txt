Refactor and enhance the Conversational BI Streamlit application based on the current project directory (`conversational_bi/`). Implement the following updates across the codebase:

### 1. Logo & Branding
- Display the logo from the root directory (`EXL_Logo.png`) prominently in the Streamlit UI (e.g., at the top of the sidebar or main header) with appropriate sizing.

### 2. Code Consolidation (`ui.py` into `app.py`)
- Merge all rendering components, layout helpers, and UI logic from `core/ui.py` directly into `app.py`.
- Remove references/imports to `core/ui.py` in other modules, ensuring `app.py` directly handles the rendering layer.

### 3. User Guidance Section
- Add an expandable or prominent section titled **"How to Use"** detailing clear, step-by-step user instructions:
  1. Uploading data (CSV/Excel).
  2. Reviewing automated EDA and dynamic KPIs.
  3. Inspecting initial trends and charts.
  4. Interacting via natural language questions using the conversational chat interface.

### 4. Automated Visual Trends & Charting
- Once a dataset is uploaded, automatically detect key temporal or numerical columns.
- Render initial trend visualizations (e.g., time-series lines, top categorical distributions, or numerical correlation heatmaps using Plotly/Altair) before the user even begins chatting.

### 5. Enhanced Statistical EDA (`core/eda.py`)
- Expand the current exploratory analysis function to compute:
  - Descriptive statistics (mean, median, standard deviation, IQR, skewness).
  - Data quality checks (exact null percentages, duplicate row counts, zero-variance columns).
  - Correlation pairs and potential outlier flags.
- Package these findings cleanly into structured metadata for display and LLM context.

### 6. Dynamic, LLM-Driven KPIs on the Dashboard
- In addition to standard high-level metrics (total records, column count, null percentage), integrate an LLM routine in `core/llm_engine.py`:
  - Pass the dataset schema and summary statistics to the LLM.
  - Instruct the LLM to dynamically determine and return 3–5 domain-relevant KPIs with business interpretations based on the detected data structure.
- Render these dynamic metrics using `st.metric()` or custom metric cards in the dashboard view.

Please maintain clean modularity for `core/data_ops.py`, `core/eda.py`, and `core/llm_engine.py`, ensure graceful error handling for missing files or unexpected data types, and update `app.py` as the unified entry point.
