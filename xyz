from sklearn.metrics import mean_absolute_error, mean_squared_error
import numpy as np

mae_sarimax = mean_absolute_error(test['sales'], sarimax_pred)

rmse_sarimax = np.sqrt(
    mean_squared_error(test['sales'], sarimax_pred)
)

mape_sarimax = np.mean(
    np.abs((test['sales'] - sarimax_pred) / test['sales'])
) * 100

print("SARIMAX Performance")
print(f"MAE  : {mae_sarimax:.2f}")
print(f"RMSE : {rmse_sarimax:.2f}")
print(f"MAPE : {mape_sarimax:.2f}%")



import matplotlib.pyplot as plt

plt.figure(figsize=(14,5))

plt.plot(test.index,
         test['sales'],
         label='Actual')

plt.plot(test.index,
         sarimax_pred,
         label='SARIMAX Prediction')

plt.legend()

plt.title("SARIMAX Forecast")

plt.show()



mae_xgb = mean_absolute_error(y_test, pred)

rmse_xgb = np.sqrt(
    mean_squared_error(y_test, pred)
)

mape_xgb = np.mean(
    np.abs((y_test - pred)/y_test)
) * 100

print("XGBoost Performance")
print(f"MAE  : {mae_xgb:.2f}")
print(f"RMSE : {rmse_xgb:.2f}")
print(f"MAPE : {mape_xgb:.2f}%")


plt.figure(figsize=(14,5))

plt.plot(y_test.index,
         y_test,
         label="Actual")

plt.plot(y_test.index,
         pred,
         label="XGBoost Prediction")

plt.legend()

plt.title("XGBoost Forecast")

plt.show()


import pandas as pd

comparison = pd.DataFrame({

'Model':['SARIMAX','XGBoost'],

'MAE':[mae_sarimax,mae_xgb],

'RMSE':[rmse_sarimax,rmse_xgb],

'MAPE':[mape_sarimax,mape_xgb]

})

print(comparison)